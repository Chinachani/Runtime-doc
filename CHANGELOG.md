# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；
版本号遵循语义化版本。买家通过管理台「更新检查」或本页获取新版本信息。

## [0.12.1] - 2026-09-12

### 架构演进与体验优化：B 站音视频解析独立外部插件试点（plugin.bili 1.3.0）与运行时网关富媒体能力对齐

- **独立外部插件生态先行探索（plugin.bili 1.3.0）**：
  - **解耦独立为标准外部插件**：按照纯正普通外部插件规范将 B 站音视频解析功能从内置体系完整解耦，不走任何官方内部特权通道；配置与缓存分别存储于插件专属目录（`config_dir/config.json` 与 `data_dir/cache`），彻底解耦 Runtime 内部 SQLite；
  - **运行时插件网关门面对齐**：`PluginGateway` 补齐 `upload_media_file` 映射方法，使外部插件在进程内开发调试与沙箱 worker 隔离环境下均具备一致透明的 QQ 开放平台富媒体文件上传能力；
  - **现代化卡片与超长音频双保险降级**：生成精简卡片交互按钮（第一排 `[下载视频]` 与 `[获取音频文件]`，第二排 `[网页打开]` 与 `[解析菜单]`），在语音上传遇时长超限或平台拒绝时自动无缝降级为普通文件（`file_type=4`）发送；
  - **独立管理面板与官方提交流水线校验**：自带自适应暗黑/明亮质感 H5 独立控制台，通过标准 action RPC 进行安全通信；新增 `tools/build_bili_plugin.py` 一键打包工具并通过 `validate_plugin_submission.py` 100% 官方严格校验，同步收录至插件市场 `plugins/market.json`。

## [0.12.0] - 2026-09-12

### 功能增强与体验优化：水印清理 0.3.0 AI 溯源等待窗口、Pillow 本地降级、富媒体异常自动普通文件转发与检测记录控制台

- **水印清理与 AI 溯源分析优化（runtime.watermarks 0.3.0）**：
  - **交互式 AI 溯源等待窗口**：发送 `/wm detect`（或 `/wm 鉴定`、`/wm 溯源`）不带附件时，开启交互式检测等待窗口，引导用户在有效期内直接发图，自动执行 C2PA 签名、EXIF 隐私、AI 生成元数据综合鉴定并输出详细报告；
  - **Pillow 本地降级容错**：未配置或微服务不可用时，自动降级至内置 Pillow 图像解析与正则特征引擎，对 JPEG/PNG/WebP 图像直接执行本地元数据剥离，保障基础脱敏功能全天候可用；
  - **富媒体发送异常自动降级普通文件（file_type=4）**：当以图片（`file_type=1`）向 QQ 官方网关发送脱敏文件遇到格式不支持或大小超出时，自动无缝降级为普通文件上传回传；
  - **检测记录分页与管理台数据看板**：支持通过指令 `/wm records [page]`、`/wm records clear` 分页浏览或清空多媒体被动检测记录；新增控制台端点 `/api/watermarks/page/data` 与 `/api/watermarks/page/data/delete`，在管理台无缝展示检测记录列表与支持批量清空。

## [0.11.1] - 2026-09-11

### 体验优化与功能增强：B站与抖音直接发送音频文件及卡片双按钮支持

- **音视频解析指令与卡片交互完善**：
  - **支持直接发送音频文件**：B站与抖音均支持 `/bili audio_file <url>`、`/dy audio_file <url>`（亦支持参数 `file` 或中文 `/bili 音频文件`、`/dy 音频文件`），直接以普通文件（`file_type=4`）上传发送，彻底避免语音时长的硬性限制；
  - **卡片按钮提供双模式选择**：视频解析卡片按钮第一排同步提供「下载视频」、「发送语音」与「音频文件」三个清晰选项；
  - **任务防抖健壮性提升**：强化任务去重与防抖键（`_task_key`）对文件发送模式的区分，保持与语音提取模式的完全独立与向后兼容。

## [0.11.0] - 2026-09-11

### 功能增强与问题修复：音视频解析 1.3.0/0.3.0 超长音频降级、卡片按钮精简与抖音创作者及删除修复

- **音视频解析与数据管理优化（runtime.bili 1.3.0 & runtime.dy 0.3.0）**：
  - **超长音频自动降级普通文件发送**：针对 QQ 官方对语音/音频消息（`file_type=3`）的时长严格限制，当抓取或提取长音频遇「上传音频时长超过限制」时，自动无缝降级为普通文件（`file_type=4`）秒级上传发送，彻底解决长音频发送失败；
  - **卡片按钮精简与统一**：B 站与抖音解析卡片移除多余的「任务列表/任务管理」和「文件列表」按钮，升级统一为首排「下载视频」与「获取音频文件」，第二排保留网页打开与菜单，交互更简洁纯粹；
  - **抖音创作者与详细元数据修复**：全面兼容字符串与字典形式创作者昵称（带抖音号），并补充解析视频文案备注、视频时长、发布时间、背景音乐及点赞/评论/收藏/转发统计；
  - **管理台与后台单条/批量删除修复**：修复控制台 `pluginId` 路由映射与纯字符串文件/任务标识解析问题，修复批量删除时出现的 404 与“目标标识无效”错误，增加端点自动降级与 404 幂等删除容错。

## [0.10.0] - 2026-09-11

### 功能增强与体验优化：绘图服务 0.3.0 免抠透明底与防改写注入、音视频解析二级缓存与安全对齐、全插件卡片按钮防截断重构

- **绘图服务（plugin.drawimg.service 0.3.0）免抠透明底、防改写注入预设、优化词回显与参考图智能规整**：
  - **透明底图免抠预设**：新增个人预设 `transparent`（默认关闭），支持通过 `/dg bg [on/off/toggle]` 或在尺寸画质卡片上一键切换；开启时自动在 Prompt 追加纯绿幕指令，生图完成后由 Pillow 精确识别并剔除绿幕像素，转换为带透明 Alpha 通道的 PNG 图片；
  - **提示词防改写注入预设**：新增个人预设 `prompt_guard`（默认开启），支持通过 `/dg guard [on/off/toggle]` 自由切换；开启时向前置注入严格遵循指令前缀，杜绝上游模型擅自篡改用户提示词；
  - **模型优化提示词（`revised_prompt`）回显**：在绘图结果卡片中自动提取并展示上游模型优化后的提示词，并提供「用优化词生图」一键快捷生图按钮；
  - **参考图前置 Pillow 压缩规整**：改图模式在将参考图上传至上游前，自动进行 EXIF 方向纠正、等比缩放至最大边 1536px 并对齐长宽为 16 的倍数，体积压缩至 4MB 安全范围，彻底避免尺寸不合规导致的 400 报错。
- **音视频解析（runtime.bili 1.2.0 & runtime.dy 0.2.0）富媒体秒发二级缓存、QQ 体积安全对齐与 Cookie 活性诊断**：
  - **富媒体秒发二级缓存**：对近期已下载并发送给 QQ 的视频与音频建立短时文件缓存，相同链接在短时间内重复请求时秒发 QQ，大幅提升并发性能并减少重复转码与流量消耗；
  - **QQ 媒体 28MB 安全上限自适应画质对齐**：视频解析依据码率与时长前置估算体积，超出 QQ 官方机器人 28MB 上传上限时自动对齐降级至最高可用清晰度，避免生硬下载失败；
  - **Cookie 活性与风控智能诊断**：在 `page_data` 中加入对 B 站（SESSDATA 完整性检测）与抖音 Cookie 登录状态与健康度的轻量诊断，便于控制台直观掌握可用性。
- **全插件卡片按钮防挤压截断排布重构**：
  - 针对 QQ 客户端单行 4~5 个按钮时文字被挤压截断（显示为 `...`）的视觉问题进行全量排布重构；
  - 绘图（`dg` / `drawimg`）、B 站（`bili`）、抖音（`dy`）、New API（`na`）、表情包（`meme`）及 Word转PPT（`word2ppt` 0.31.1）的选项、分页导航与管理操作按钮统一规范为每排最多 2~3 个宽版按钮，严格控制卡片总行数不超过 QQ 官方 5 行上限。

## [0.9.0] - 2026-09-11

### 功能增强与体验优化：绘图服务 0.2.0 画幅画质重构与失败反馈、音视频解析批量删除、QQ 开放平台外链与图表零下曲线修复

- **绘图服务（plugin.drawimg.service 0.2.0）比例画质综合卡片与失败通知**：
  - 参考 `gpt_image_playground`，重构尺寸与画质设置交互为一体化卡片：支持主流画幅比例一键选择（`1:1`、`16:9`、`9:16`、`4:3`、`3:4`、`3:2`、`2:3`、`21:9` 及 `auto`）与画质选择（`auto`、`standard`、`hd`、`high`），交互点击即时回显 `✓` 勾选状态并即时刷新卡片；
  - 自动将用户所选比例映射为标准安全分辨率并在接口支持时附带 `aspect_ratio` 参数，遇到上游模型参数限制时自适应降级重试；
  - 优化异步生图任务的失败处理机制，生图异常时自动全额退款并向用户发送友好通知；针对 QQ 官方 `msg_id` 5 秒过期问题引入主动重试降级机制，确保失败提示必达用户。
- **抖音解析（runtime.dy 0.1.0）与 B 站解析（runtime.bili 1.1.0）文件管理增强与 404 修复**：
  - 彻底修复 `DouyinService` 删除包含中文、空格或特殊字符文件时因未转义导致的 404 错误，引入 `urllib.parse.quote` 编码防护并对已删除文件提供优雅容错；
  - 后端 `/api/dy/page/data/delete` 与 `/api/bili/page/data/delete` 扩展支持 `identifiers` 批量删除参数；
  - 前端控制台插件数据表格新增行复选框、表头全选、批量删除已选项目与一键删除当前页功能，并附带操作确认弹窗。
- **QQ 接入页面增加 QQ 开放平台外链跳转**：
  - 控制台「QQ 接入」操作栏新增前往 QQ 开放平台（`https://q.qq.com/`）官方控制台的外链跳转按钮，支持新标签页安全打开。
- **运行概览事件趋势图零下曲线修复**：
  - 优化控制台消息处理趋势图表，将 Area 曲线的插值方式由 `natural` 修正为单调平滑插值 `monotone`，并限制 Y 轴下界为 `0`，消除平滑曲线在低值区间向下过冲出现负数刻度的异常现象。

## [0.8.3] - 2026-09-11

### 问题修复：C2C 单聊底栏校验友好化、远端快照冲突闭环与 CI 流水线并发竞争修复

- **C2C 底栏参数校验与字数提示优化**：
  - 彻底优化 `runtime/panels.py` 中 `validate_c2c_menu` 校验，拆分排查菜单名称为空、名称字符字节长度超限（一级菜单最多 10 字符/5 汉字，二级菜单最多 14 字符/7 汉字）、类型无效及缺少内容等场景，提供精确定位到项序与具体字段的友好错误信息，消除笼统的“菜单名称或类型无效”报错；
  - 控制台 C2C 底栏编辑器增加即时字节数计算与汉字限制提示，超出上限即时红色高亮警告，并在保存前提供前置表单检查。
- **C2C 底栏远端快照冲突与强制覆盖闭环**：
  - 新增 `POST /api/qq/c2c-menu/remote/adopt` 接口，支持一键读取 QQ 远端底栏配置并同步更新本地草稿与 `remote_snapshot`，消除本地快照脱节问题；
  - `POST /api/qq/c2c-menu/apply` 接口支持 `force: true` 强制覆盖模式；当检测到远端已被外部修改（HTTP 409）时，控制台弹出冲突处理对话框，引导用户选择「读取 QQ 端并同步快照」或「强制覆盖发布」；
  - 完善 `storage.py` 中 `set_c2c_menu_remote_state` 的 UPSERT 逻辑，并在初次未配置菜单时安全回退，防止接口抛出异常。
- **CI 流水线并发创建 Release 竞争与日志误报修复**：
  - 修复推送 Tag 时 `build-apk.yml` 与 `release-image.yml` 并发向 `Chinachani/Runtime-doc` 创建 Release 导致的 HTTP 422 竞态冲突，在 `tools/create_doc_release.py` 与 `tools/upload_doc_asset.py` 中增加自动退避重试获取 release_id 并转为 PATCH 的重试机制；
  - 修复 `tools/create_doc_release.py` 中错误日志输出打印历史 404 响应体的变量引用 Bug。

## [0.8.1] - 2026-09-11

### 问题修复与发布加固：文档发布流水线非 ASCII 路径转义修复

- **文档发布与同步流水线 URL 转义修复**：
  - 修复因中文文件名（`docs/插件开发指南.md`、`docs/插件上架指南.md`）未作 RFC 标准 URL 转义导致的 `UnicodeEncodeError`；在 `create_doc_release.py`、`sync_plugins_to_doc.py` 与 `upload_doc_asset.py` 中引入完整的 `urllib.parse.quote` 编码防护。

## [0.8.0] - 2026-09-11

### 架构演进：第三方插件开放生态、控制台一键提审与自动化审查流水线

- **插件开发者指南与上架生态全景文档**：
  - 新增 `docs/插件开发指南.md`，深度覆盖外部插件目录规范、`plugin.toml` 清单要求、`setup(context)` 生命周期、QQ 官方指令与事件分发、Markdown 卡片交互、定时调度及独立 Web 页面 RPC 桥接。
  - 新增 `docs/插件上架指南.md`，规范市场准入合规标准、`market.json` 索引格式及镜像加速方案。
- **管理台内置插件一键提审功能**：
  - 后端提供 `/api/plugins/market/prepare-submit` 接口，自动在内存中安全扫描本地已安装外部插件并打包，安全剔除缓存与临时文件，精确计算 SHA-256 哈希值与字节体积，校验权限与依赖，并自动生成标准化 GitHub Issue 提审链接及 `market.json` 结构配置。
  - 前端管理台深度集成提审模态框（`<Dialog>`），支持在已安装插件列表与插件市场双入口一键发起入驻申请，极大降低开发者入驻门槛。
- **公开文档仓库 CI 自动化审查流水线**：
  - 提供 `tools/validate_plugin_submission.py` 自动化校验工具，集成 ZIP 安全性与结构校验。
  - 提供 GitHub Issue Form 模板与 Actions 工作流，支持在 `Chinachani/Runtime-doc` 仓库全自动校验提审 Release ZIP、验证哈希与权限并自动合并入库。

## [0.7.4] - 2026-09-11

### 问题修复：可视化聊天表情光标精准无换行嵌入与绘图充值双倍到账彻底消除

- **可视化聊天（Visual Chat）表情光标精准嵌入与移除冗余换行**：
  - 彻底移除表情插入时默认附带的强制换行符（`\n`），支持在输入框光标当前选中位置平滑插入单个或多个表情符号，插入后光标自动后移且维持输入框焦点，打字连贯自然。
- **绘图服务插件（plugin.drawimg.service v0.1.4）充值双倍到账彻底消除与防御加固**：
  - **权威账本解耦与多重记账消除**：在支付成功回调 `on_payment_success` 中彻底阻断任何对 `_change_balance` 或 `points.credit` 的二次入账逻辑，统一由 `runtime.pay` 先期权威入账，插件仅拉取并刷新最新余额缓存与下发到账通知；即便在无全局积分模块的降级模式下也仅更新插件内部结构，杜绝一切导致双倍记账的可能。
  - **插件版本升级与市场归档**：按规范递增版本号至 `0.1.4`，重新打包生成分发 ZIP 包，并更新官方市场 `market.json` 校验摘要与文件体积。

## [0.7.3] - 2026-09-11

### 问题修复与体验升级：可视化聊天表情选择器、媒体上传智能识别、QQ 网关停止彻底断连、7天运行指标持久化及控制台浅色美化

- **可视化聊天（Visual Chat）内置表情选择器**：
  - **分类表情面板**：重构输入框表情按钮，由单表情插入升级为轻量美观的 `EmojiPicker` 弹窗，支持表情神态、手势人物、符号生活、动植物与食物等高频分类展示与即时搜索，点击即可无缝将表情插入输入框光标位置。
- **可视化聊天媒体与附件上传自适应与网络通道加固**：
  - **选项精简与后缀智能识别**：移除原有的手动“图片/视频/语音/文件”下拉选择，统一根据上传文件名后缀与 MIME 类型自动推断，范围外的其他非标准扩展名一律自动按文件（`file_type=4`）规范处理，前端同步展示带有类型与体积徽标的识别状态。
  - **移动端上传通道修复**：将原本直接调用浏览器原生 `fetch` 改为基于 `runtimeApi` 管道，自动解析激活节点 `baseUrl`、注入 Bearer Token / CSRF 并携带完整鉴权凭据，解决移动端/远程节点上传进度始终卡在 0% 的连接问题。
  - **网页端上传与发送阻塞修复**：修复当媒体文件已存在于服务端且状态为 `sent` 时客户端轮询等待 `ready` 状态超时卡在 100% 的问题，并在发送接口 `/api/media/uploads/{key}/send` 中允许再次发送已就绪/已发送的媒体，确保历史缓存文件可即时发出。
- **QQ 接入网关停止与事件完全阻断修复**：
  - **网关生命周期与 WebSocket 连接销毁**：修复在控制台停止 QQ 网关后底层 WebSocket 仍保持长连并继续接收分发消息的问题；`RuntimeQQClient` 重写连接追踪与 `close()` 显式关闭所有活跃 aiohttp WebSocket 连接与运行协程，并在 `_dispatch` 及所有事件入口前置操作态守卫校验，确保网关停止后不再分发或回复任何事件。
- **管理台运行指标 7 天持久化与真实趋势接入**：
  - **SQLite 周期聚合持久化**：新增 `metrics_hourly` 数据库表与索引，由 `RuntimeMetrics` 异步缓冲并周期落盘各小时段的接收、回复、错误、指令及限流指标，支持服务重启后指标累加与最长 7 天滚动留存清理（自动修剪）。
  - **全局真实指标替换演示数据**：移除 `/dashboard` 概览页所有的静态模拟/演示数据（Mock Data），接入真实的 24 小时消息处理趋势与持久化指标快照。
- **管理台浅色主题视觉质感优化**：
  - **明亮卡片与层级重构**：去除浅色模式下概览卡片底部灰暗沉闷的阴影渐变，重构为通透纯净的亮白卡片背景与柔和外边框；为核心指标提供柔和色彩徽标背景与高对比度状态标签，大幅提升白日模式视觉体验。
- **绘图服务插件（plugin.drawimg.service v0.1.3）充值双倍到账根治**：
  - **多重记账拦截**：修复在支付成功事件触发时，因 `runtime.pay` 统一先期入账 `points` 权威账本后插件内部再次调用 `_change_balance` 导致的重复翻倍记账 Bug；现仅同步刷新本地余额缓存与下发到账通知。
  - **通知文本优化**：美化充值成功提示信息，区分在线支付充值与管理员后台加币文案。
  - **插件版本升级与市场归档**：按规范递增版本号至 `0.1.3`，重新打包生成并更新官方市场 `market.json` 校验摘要。
- **协作开发文档与插件版本号约束强化**：
  - 在 `AGENTS.md` 中确立硬性开发规范：无论是内置插件还是外部插件，任何代码修正或特性增改发版均须严格依照主干规则（Bug 修复 Patch +0.0.1，新功能 Minor +0.1.0）递增插件版本号，并同步更新元数据清单与重新归档市场分发包。

## [0.7.2] - 2026-09-11

### 问题修复与体验完善：水印清洗历史重置、SteamWatch 提及解析与底图恢复、移动端登录持久化及插件市场生态净化

- **水印服务插件（watermarks）清洗记录自动重置**：
  - 清洗操作在执行文件发送或取消交互后，自动清空暂存的文件队列 `_pending_files`，行为对齐绘图服务插件的多图合成后清理逻辑，避免重复发送或状态残留。
- **Steam 轮询监控插件（SteamWatch）体验与跨场景解析强化**：
  - **用户提及跨 OpenID 智能解析**：解决群聊中 `/sw bind`（基于 `user_openid`）与 `/sw query @用户`（提及为 `member_openid`）之间的 OpenID 不匹配问题，支持跨键关联查询与双向绑定映射。
  - **停游轮询通报底图渲染恢复**：当玩家停止游戏时，在通知生成阶段传递 `last_appid_int` 与 `avatar_url`，并优化底图优选逻辑，优先使用该游戏官方 Steam 封面图，确保停游通知与开始通知同样具备完整精美的卡片底图。
- **移动端 Android App 登录状态持久化与原生沙箱页面渲染**：
  - **入口登录态持久化网关**：移动端客户端重构客户端根入口引导，从本地存储检测已保存的节点信息与 Token 并自动校验免密恢复进入控制台，彻底解决每次冷启动/重新切回 App 都需重复输入凭据登录的痛点；仅在用户主动点击退出时清理凭据。
  - **沙箱插件页面移动端全兼容**：针对 Capacitor/Android WebView 环境下同源限制与 CSP 拦截问题，通过授权安全代理抓取页面内容，并采用安全隔离的 `srcDoc` 沙箱 iframe 渲染，配合后端动态扩展 `frame-ancestors`，使外部插件管理页面在 App 移动端正常呈现。
- **插件市场体系规范与官方物料源净化**：
  - **插件源纯净化**：官方插件市场仅保留 `plugin.demo.guide`、`plugin.drawimg.service`、`plugin.word2ppt` 三个已公开且可直接分发的外部插件包，移除未就绪条目与自用私有插件 `plugin.selfauth`。
  - **自动同步流水线工具**：优化 `tools/create_doc_release.py` 与新增 `tools/sync_plugins_to_doc.py` 工具，标准化 `market.json` 校验摘要生成并打通向 Runtime-doc 公共仓库 Release 资产的自动同步管道。

## [0.7.1] - 2026-09-10

### 体验革新：Web 一键更新防断连状态机、App 一键更新代理加速、部署环境安全加固与插件商店

- **Web 管理台一键更新体验优化（彻底根治重启掉线假死痛点）**：
  - **实例生命周期跟踪（boot_id）**：FastAPI 后端进程启动时动态生成唯一 `boot_id` 与启动时间戳，并在 `/api/health` 读数中输出，彻底区分「旧容器仍在拉取镜像」与「新版本容器启动就绪」。
  - **分段阻断模态状态机**：前端下发更新指令后，开启多阶段状态机弹窗与计时器：
    - 阶段一【镜像拉取中】：检测到旧容器仍在运行时保持锁屏，动态显示已耗时与拉取指令，禁止提前关闭弹窗或刷新；
    - 阶段二【容器重启中】：旧容器销毁、网络断连瞬间自动转入「新镜像拉取就绪，容器正在重新创建启动」状态；
    - 阶段三【新版启动完成】：新容器健康检查通过且 `boot_id` 换代后，提示「新版本启动成功」并安全平滑重载页面。
- **Android App 客户端一键更新（内置 gh-proxy.com 国内高速代理）**：
  - **双通道极速下载**：管理台版本检测接口与系统管理页面内置「⚡ 国内高速加速代理（`https://gh-proxy.com/`）」与「🌐 GitHub Releases 官方源」双通道，国内用户可在 App 端一键下载最新 APK 安装包。
  - **直链复制与安装器唤起**：提供一键复制加速直链与外部浏览器/系统下载管理器无缝唤起，下载后即可覆盖升级。
- **一键部署脚本（install.sh）安全机制与环境预检加固**：
  - **端口冲突实时预检**：利用 `ss`、`netstat`、`lsof` 实时检测管理台外部访问端口（如 8080）是否已被宿主机其他服务占用，并输出占用进程信息，支持自主更换或强制覆盖。
  - **磁盘剩余空间硬性拦截**：根分区可用空间小于 2GB 时强制拦截，防止解压 Docker 镜像途中耗尽磁盘；小于 5GB 时输出告警并给出清理建议。
  - **DNS 域名解析与网络健康探测**：提前探测 `ghcr.1ms.run`、`ghcr.io` 等镜像源域名的 DNS 解析，若解析失败输出诊断建议与公共 DNS（223.5.5.5 / 119.29.29.29）指引。
  - **防火墙规则检测与放行建议**：自动检测 UFW 与 firewalld 防火墙状态，输出针对性放行命令并在安装完成卡片中重点提示，杜绝“部署成功但打不开管理台”的问题。
  - **管理员密码复杂度校验**：输入密码小于 8 位时提示弱密码风险。
- **插件市场 / 插件商店架构落地（解耦外置插件，按需即装即用）**：
  - **新增独立插件市场模块（runtime.plugins_market）**：提供远端市场清单拉取、内置离线官方清单兜底、本地已安装状态自动标注（已安装/可升级/未安装）。
  - **国内代理加速流式下载与 SHA-256 完整性校验**：市场一键安装支持 `gh-proxy.com` 镜像加速，校验安装包哈希，无缝接入现有 Bubblewrap 沙箱与权限声明管道。
  - **前端管理台双 Tab 模式**：插件管理页面新增「已安装插件」与「插件市场 (Plugin Store)」Tabs，提供搜索、分类筛选、所需权限预警与一键安装升级弹窗。

## [0.7.0] - 2026-09-10

### 架构演进与插件交互全流程优化：水印清理增强、Steam 监控卡片重构与独立解析服务

- **水印清理插件（runtime.watermarks）全流程优化**：
  - **富媒体引用与回复穿透提取**：`_attachment_candidates` 支持递归扫描 QQ 被引用/回复消息（`message_reference`、`ref_msg`、`quote`、`source_msg` 及嵌套 `msg_elements`），群内直接回复带图消息并发送 `/wm clean` 或 `/wm detect` 即可无感处理，无需重新上传文件。
  - **文件首发交互引导卡片**：当随图片/文件发送裸指令 `/wm` 时，自动弹出操作卡片，提供「🧹 清理此文件水印」与「🔍 仅检测标记」一键操作，无需手动重新补全指令。
  - **等待窗口交互卡片与取消指令**：发送裸指令 `/wm clean` 开启文件等待窗口时，返回交互引导卡片并支持 `/wm cancel`（或点击「❌ 取消等待」按钮）随时关闭等待状态。
  - **上游服务异常友好诊断**：拦截 `watermarks-remover` 连接失败（ConnectError、TimeoutException），由原本晦涩的 internal 错误转为包含服务配置与容器排查建议的指引。
- **Steam 监控插件（SteamWatchService）菜单与操作逻辑重组**：
  - **分级菜单重构与去重**：主菜单重构为 4 行 × 3 列均衡布局，精简重复项（剔除同时存在的 `/sw me` 与 `/sw query me`），整合「状态查询 / 订阅推送 / 账号绑定」、「监控管理 / 网络连通 / 完整帮助」、「我的状态 / 详细资料 / 绑定账号」以及「监控列表 / 当前订阅 / 添加监控」。
  - **未绑定引导卡片**：未绑定用户触发 `/sw me` 时弹出专属引导卡片，提供「🔗 立即绑定账号」填入按钮与「📖 绑定说明」一键直达，杜绝冷冰冰的纯文本报错。
  - **绑定成功与查询反馈卡片化**：绑定成功后返回包含下一步操作（查看我的状态、查看详细资料）的操作卡片；查询与详细资料结果支持快捷操作按钮（刷新、切换简要/详细、查询他人）。
- **一键部署脚本升级与独立解析服务架构**：
  - **最低配置建议与硬件嗅探**：`install.sh` 启动时自动探测主机 CPU 核心数、内存及磁盘空间，提供硬件规格评估与部署建议。
  - **4 档预设部署方案**：提供增强媒体型（默认，全功能媒体与三方解析，推荐 ≥2C4G）、核心轻量型（1C1G 极速运行）、全家桶型（含沙箱环境）以及自定义组件勾选。
  - **Bilibili / 抖音解析服务独立开源**：将解析 API 拆分为独立的轻量容器镜像（`Chinachani/bilibiliwatch-api` 与 `Chinachani/douyinwatch-api`），提供多架构自动化 Docker 构建工作流，与核心框架网络隔离部署。

## [0.6.23] - 2026-09-10

### 绘图积分收尾：概览汇总落地与机器人侧余额键对齐

- **概览"积分余额"恒为 0**：v0.6.20 将 `page_overview_data` 的 `stats.total_credits` 置 0 占位（原计划在 users 异步路径回填但未实现），后台概览卡显示 0。现在该方法改为异步并直接汇总 `point_accounts`（point_type=drawimg）权威账本，保存配置的返回体同步更新。
- **机器人侧余额读数与后台不一致（如后台 26、`/dg status` 报 15）**：`_user_key` 生成 `qq_official:<id>` 前缀键，而账本与后台统一使用裸用户编号（前缀键在存量迁移时已并入裸键），导致机器人侧扣费/查询落在带前缀的幽灵账户上。`_user_key` 现经 `_clean_user_key` 归一，扣费、退款、状态、赠送、支付 owner_key 全部与后台同一身份。
- 附带核对：管理台"操作过于频繁，请 N 秒后重试"来自 `require_plugin_action` 的 20 次/60 秒滚动窗口（含用户/模型/黑名单/预设等全部写操作），计数偏快时提示按设计出现，非缺陷。
- 回归测试：`page_overview_data` 异步化适配；机器人侧余额/扣费/绑定/支付断言改用裸键。

## [0.6.22] - 2026-09-10

### Steam 通知被吞修复：分组订阅无目标回退全局

- **根因（219 实例实锤）**：`notify_group_enabled=true` 但 `steamid_groups=[]`（分组从未配置成功），分组扇出结果为空——通知无目标、无日志、无提示，表现为"轮询正常但从不推送"。现在分组模式查无目标时自动回退全局 `notify_targets` 并记录 warning，配置错误不再静默。
- 顺带：群模式回退路径同样适用于监控号池全部无分组的场景，与旧插件"分组外还有全局兜底"的行为对齐。
- 回归测试：无分组时回退全局目标并成功推送。

## [0.6.21] - 2026-09-10

### Steam 背景图直连回退与轮询通知可靠性

- **游戏背景图获取不到（根因：代理）**：背景图下载此前完全走 `proxy_url`，219 的 Clash 节点整体不可达时（连 Google 都不通），Steam CDN 下载随之失败。Steam 图片 CDN（cloudflare/akamai）国内通常可直连而 Steam Web API 不行，现在改为**直连优先、失败再走代理**，代理抖动不再拖垮背景图。
- **轮询无通知**：`_summaries` 失败时保留 `_last_state` 基线并记录 warning 日志，避免恢复后按"首轮"重新建档导致漏发/补发；通知发送失败从静默 `debug` 提升为 `warning`——场景未启用、限流、授权过期等此前完全不可见，"轮询正常但没收到通知"从此有迹可循。
- 新增回归测试：API 失败保留基线、恢复后只发一次通知；配置死代理时背景图直连成功。

## [0.6.20] - 2026-09-10

### 绘图积分统一：权威账本单源化

- **余额来源统一**：网页后台用户列表与积分调整统一读取 `point_accounts` 权威账本（point_type=drawimg）；credits.json 多份历史副本（含套娃路径的 45/20/5 矛盾值）只用于发现用户存在，不再参与余额计算。
- **写路径收敛**：后台调整积分不再向 5 个路径散写 credits.json 副本（正是副本失步导致"后台改了、面板不变"的根因），同时删除直接改写 SQLite 的旁路——全部经 PointsService 账本（带 operation_id 幂等与流水审计）。
- **部署侧清理**：219 上全部历史 credits.json 副本已备份后删除（备份在服务器 /root/wm-cleanup-backup）。

## [0.6.19] - 2026-09-10

### SteamWatch 1.28 移植补全：二次确认、图片渲染与按钮断行

- **危险操作二次确认（confirm_destructive，默认开）**：`/sw remove`、`/sw unbind`、`/sw unsub`、`/sw subclean` 现在返回确认卡片，按钮点击即以 `/sw confirm <token>` 完成执行；凭据 5 分钟内一次性有效，按发起人 + 会话绑定，他人点击/跨会话/重放/越权均明确拒绝。关闭开关后恢复直接执行。
- **图片渲染（磨砂卡片）**：查询结果与状态通知可渲染成 Steam 风格图片——游戏头图/商店背景 + 高斯模糊半透明信息卡。开关 `render_as_image` / `render_image_in_notify`（默认关，与旧版一致）；渲染或上传任一步失败自动回退纯文本，不影响其他目标推送。
- **中文字体内置**：Noto Sans CJK SC 随镜像分发（新增 `sw-image` 可选依赖组，仅 Pillow），开箱即用无需系统字体；`image_font_builtin` 开关控制，仍支持 `/sw font dl|set|clear` 自定义字体管理（管理员）。
- **按钮断行修复**：QQ 卡片四字按钮一行最多渲染 3 个，超过会被截断显示不全；主菜单与各模块子菜单按此规则自动断行（导航行同步适配）。
- 完整测试覆盖：确认卡全流程（他人点击拒绝/防重放/管理员门禁）、真实 PIL 渲染（内置字体复制、磨砂卡片尺寸）、按钮断行。

## [0.6.18] - 2026-09-10

### 指令合并：detect 与 clean 双统一入口

- **检测全部合并到 `/wm detect`**：纯文本查隐藏字符；与图片/文件同发查元数据水印（EXIF、C2PA、AI 标记），结果卡片均可带「一键清理」。
- **清理全部合并到 `/wm clean`**：纯文本清理（格式保留可复制）；与文件同发直接清理并回传；无参数发送进入 2 分钟等待窗口（`file_wait_seconds` 可调），窗口内首个文件自动清理回传，**再次发送 `/wm clean` 即重置窗口**。
- 旧指令 `/wm inspect`、`/wm file` 保留为兼容别名；旧确认按钮 `/wm file confirm` 映射到暂存清理。菜单按钮同步更新。

## [0.6.17] - 2026-09-10

### /wm detect 文本文件双模式

- **`/wm detect` 统一入口**：与图片/文件同发时检测元数据水印（EXIF、C2PA、AI 标记，不改动文件）；纯文本时检测隐藏字符——与 `/wm inspect` 同源但结果卡片同样附带「一键清理」按钮，无需区分指令。

## [0.6.16] - 2026-09-10

### 水印文件回传修复与结果可复制化

- **修复"网关不支持回传文件"**：QQ 网关在 Runtime 上的属性名是 `qq` 而非 `gateway`（笔误导致永远判定不支持）；回传链路对齐 word2ppt 的完整三步（prepare → upload_media_file 取回 `file_info` → send_media），并按文件后缀自动选择 QQ 富媒体类型（图片/视频/语音/任意文件），非 c2c/group 场景给出明确提示。
- **清理结果保留格式可复制**：QQ 官方 Markdown 不支持代码块，改用每行前缀不换行空格的方案，缩进与换行原样保留，长按消息即可整体复制；头部注明清除了哪些标记。
- **带附件的 /wm file 直清**：主动附文件 + 指令视为明确意图，跳过确认卡片直接清理回传（等待窗口流程不变）。

## [0.6.14] - 2026-09-10

### 水印文件等待窗口与一键清理

- **文件清理等待窗口**：`/wm file`（不带文件）后进入监听窗口（默认 2 分钟，管理台 `file_wait_seconds` 可调 10-600 秒），窗口内本会话发来的第一个图片/文件自动清理并主动回传，无需再敲指令、也无需确认卡片。
- **检测一键清理**：`/wm inspect` 结果卡片的「🧹 一键清理这段话」按钮直接执行清理（原文编码在按钮数据里）；超过 QQ 按钮长度上限的长文本退回填入模式。
- **清理结果保留格式**：`/wm clean` 结果以 Markdown 代码块返回，缩进与换行原样保留，可长按直接复制；卡片附带「再检测清理结果」按钮验证清理效果。
- **被动检测开关更名**：`auto_detect` 标签改为「被动检测记录（收图自动登记，不提醒）」，避免误解。

## [0.6.13] - 2026-09-10

### Steam 监控子菜单指令按钮与 @提及参数修复

- **子菜单带指令按钮**：五个模块子菜单各配一组快捷指令按钮（每行最多 5 个，QQ 卡片上限）——管理模块（状态/列表/推荐配置/设置轮询）、通知模块（订阅/取消/订阅信息/分组列表/订阅分组）、查询模块（查自己/我的资料/查他人/详细资料/解析）、绑定模块（我的绑定/解绑/绑定）、网络模块（API 测试/代理测试）；需要参数的按钮（如"绑定账号"）填充输入框不自动发送。
- **修复 `@机器人 /sw add <steamid>` 报"分组名称不能包含冒号"**：两个叠加原因——① @机器人名（如 `@567 WIKI`）被路由按空白切碎，`WIKI` 等残片混入参数，被当成第二参数（分组名）且含冒号校验逻辑未区分"未提供分组"与"提供了非法分组"；② 结构化提及存在时用户输入的 `@昵称` 参数被误删。现在：命令参数先按结构化提及清洗（只截断命令词之前的机器人前缀），用户主动输入的 `@昵称` 不受影响；分组名校验仅在显式提供第二参数时触发。
- **`@昵称` 反查与报错语义**：`/sw add @念` 正确经昵称元数据反查；@ 结构化用户未绑定时提示"请先让该用户使用 /sw bind 绑定"而不是笼统的解析失败。

## [0.6.12] - 2026-09-10

### 水印图片回传修复、图片检测与被动检测记录

> 说明：v0.6.11 tag 被同日两次提交先后占用（sw 绑定修复与水印新功能），
> CI 仅构建了先打 tag 的那次，水印改动实际未进入 0.6.11 镜像。本版本
> 将两者一并发布，此后以本段为 0.6.11/0.6.12 的合并发行注记。

- **文件被动接收（参照 word2ppt 模型）**：发送文件给机器人即自动暂存并回执任务编号，检测/清理由用户指令触发，绝不主动打扰。
- **图片清理回传修复**：上游对二进制文件的清理结果在 `cleaned` 字段（文本才是 `file`），此前图片清理提示"未返回文件内容"，现兼容 `cleaned`/`file`/`data` 并回传清理后的文件，附处理动作摘要（如"移除 EXIF 段"）。
- **`/wm detect`（与图片/文件同发）**：只检测不改动，报告格式、C2PA/AI 元数据标记、命中明细与综合判断。
- **被动检测记录**：带可识别后缀的文件自动登记检测结果（魔数嗅探 + 后缀白名单，上限 200 条），`/wm records` 或管理台按需查看，`auto_detect` 开关控制。

## [0.6.11] - 2026-09-10

### Steam 监控绑定解析修复与分级菜单

- **修复"绑定成功但查询我说未绑定"**：0.6.8 的绑定键清洗引入回归——`_bindings()` 解析只接受三段格式（`platform:user:steamid`），而清洗后写入与存量行都是两段（`user:steamid`），全部被丢弃，导致绑定看似成功（写入了）但 `me`/`info me` 查询永远落空。解析改为按最后一个 `:` 分隔取 steamid、其余为用户键，兼容任意历史格式。
- **分级菜单（MD+按钮）**：`/sw` 主菜单改为分级入口——顶部五个模块按钮（管理/通知/查询/绑定/网络）一键进入对应子菜单，子菜单以 Markdown 呈现完整命令清单并带「主菜单 / 上一个模块 / 下一个模块」导航按钮；主菜单保留常用快捷按钮（我的账号、查询、绑定、监控列表等，管理员按钮保留权限校验）。
- **绑定写入即清洗**：绑定/解绑时把其他用户的存量行（含旧 `qq_official:` 三段格式）顺带归一化为 `user:steamid`，数据自然收敛。

## [0.6.10] - 2026-09-10

### 水印清理交互与体验完善

- **确认按钮跨体系命中暂存文件**：QQ 按钮回调的发送者 openid 与普通消息可能分属不同体系（interaction 用 group_member_openid，消息用 user_openid），导致确认清理时按发送者查找暂存失败。暂存记录现携带会话标识，回调按会话兜底命中。
- **检测结果人性化**：`/wm inspect` 的命中项不再输出原始 dict（如 `{'codepoint': 'U+200B', ...}`），转译为"零宽字符（U+200B），出现 N 次，疑似"等可读描述。
- **文本清理降级**：上游未配置 Layer B 改写策略时，`/wm clean` 不再报错，自动降级为本地隐藏字符清理（与上游 Layer A 规则一致）。
- **菜单完善**：主菜单与检测分类新增「清理文件」「支持格式」按钮；新增"支持的文件格式"页（文本/图片/文档/音视频分类说明及大小上限）。

## [0.6.9] - 2026-09-10

### 绘图插件清除平台前缀键（数据合并）

- **积分身份统一为裸用户编号**：`point_accounts` 表中旧版 `qq_official:<openid>` 身份在启动迁移时并入同名裸键（两身份都有余额时保留较大值，积分只增不减），前缀行随后删除；`/dg give`、管理台积分调整不再双写两个身份。
- **usage.json 启动迁移**：加载时把 `qq_official:<user>` 键合并为裸键（字典字段互补合并），旧前缀键删除；黑名单配置读写均归一化为裸键并去重；管理台用户列表、积分调整、模型偏好设置同步去前缀。
- **credits.json 读取合并**：同一用户的裸键与旧前缀键并存时取较大值。
- 顺带修复 `_save_usage` 对已 replace 的临时文件二次 chmod 导致的保存失败。

## [0.6.8] - 2026-09-09

### Steam 监控插件补全（对齐 AstrBot 1.28 版）

- **完整目标解析**：`/sw` 全部目标参数现在支持 `steamid64`、`好友码`（9 位 CS:GO 好友码）、`steamcommunity.com/profiles/...` 资料链接、`/id/自定义ID` 链接（经 ResolveVanityURL 解析）、`me`、`@绑定用户` 与 `@昵称` 反查；`resolve` 指令同时输出好友码。
- **info 详细查询**：`/sw info <目标>` 输出状态（离线/在线/忙碌等 7 态中文）、实名、主页、注册时间、地区，游戏中时追加该游戏总时长与成就进度（`IPlayerService/GetOwnedGames` + `GetPlayerAchievements`）。
- **分组订阅**：`/sw sub|unsub <分组>` 将当前会话订阅到指定分组；`/sw add|remove <目标> <分组>` 管理 SteamID 与分组的归属（同一账号可入多组，remove 仅移出该分组、无剩余分组才移出号池）；推送按分组扇出去重，分组模式启用时不回退全局订阅；`/sw groupinfo|grouplist` 分页查看，`/sw subclean` 同时清理无效全局与分组订阅。
- **监控列表分页**：`/sw list [页码]` 每页 8 条，显示绑定用户（优先昵称）与所属分组。
- **停止游戏推送增强**：开启 `notify_on_stop` 后推送"已停止游戏+本次游玩时长"，`show_stop_playtime_comment`（默认开）附加阶梯评价；`/sw comment on|off`、`/sw notify_on_stop on|off` 运行时开关。
- **API 请求重试**：查询 Steam API 按配置的次数与间隔自动重试。
- **模块化菜单**：`/sw manage|notify|query|bind|net` 输出各模块命令清单，`/sw` 卡片菜单新增详细资料/解析/完整菜单按钮。
- **清除绑定键平台前缀**：绑定键从 `qq_official:<openid>:<steamid>` 迁移为 `<openid>:<steamid>`（与 drawimg 的清理方向一致），存量带前缀行读取时自动剥除、用户重新绑定后即写入新格式；管理台绑定列表同步归一化显示。
- **查询本地化游戏名**：开启 `use_localized_game_name` 后按 `game_name_language` 经商店 API 换取中文名（带 TTL 缓存）。

## [0.6.6] - 2026-09-09

### 水印清理文件元数据清理与服务状态人性化

- **文件元数据水印清理**：新增 `/wm file` 指令——把图片/文件与指令一起发送，服务端下载附件后调用 watermarks-remover 清理 EXIF、C2PA、XMP 等元数据标记，并将清理后的文件回传到当前会话。默认先弹确认卡片（`require_confirm` 可关），受 `max_file_size_mb` 大小限制。
- **服务状态人性化**：`/wm status` 不再直接输出上游 JSON，改为可读描述——在线版本、文件处理能力（EXIF/C2PA/PDF/音视频工具）、文本改写（Layer B）是否配置、文本水印检测可用性。
- 主菜单与帮助文案同步补充文件清理用法。

## [0.6.5] - 2026-09-09

### 修复客户端登录 500（users 表无 role_id 列）

- **根因**：`POST /api/auth/token` 的响应体引用了 `user["role_id"]`，而 `authenticate()` 返回的是 `users` 表原始行——该表自 v0.1 起就没有 `role_id` 列（角色经 `user_roles` 关联表映射）。凭据校验成功后在序列化响应时抛出 `KeyError`，登录一律 500。浏览器端登录走 `/api/auth/login`（不引用 role_id）所以从未暴露；打包客户端上线 Bearer 登录后必现。
- **修复**：响应体移除 `role_id` 字段；`test_api_tokens` 的 mock 用户改为真实表结构作回归防护（修改前该测试因 mock 带 role_id 而无法复现 500）。
- 与 0.6.4 的 PNA 预检修复叠加后，客户端"Failed to fetch"链路的两个服务端阻断点全部消除。

## [0.6.4] - 2026-09-09

### 修复安卓客户端登录 Failed to fetch（Private Network Access）

- **根因**：安卓 13+ WebView 强制执行 Private Network Access（PNA）——从 App 的 `http://localhost` 源向局域网地址（如 `192.168.1.219`）发起的跨源请求会先发送携带 `Access-Control-Request-Private-Network: true` 的预检；Runtime 的 CORS 中间件默认拒绝该扩展预检（HTTP 400），WebView 直接以 `Failed to fetch` 终止请求。因此健康探测（简单 GET，无预检）能通、延迟显示正常，而登录的 `POST /api/auth/token` 必然失败。
- **修复**：CORS 中间件启用 `allow_private_network=True`，对 PNA 预检正确响应 `Access-Control-Allow-Private-Network: true`。仅影响打包客户端（本地源→私网）这一合法调用场景；浏览器直连管理台不受影响。
- **APK versionCode 递增**：CI 由发版 tag 自动推导 versionCode（major×10⁶+minor×10³+patch），修复 versionCode 恒为 1 导致 Android 拒绝覆盖安装的问题。

## [0.6.3] - 2026-09-09

### 水印清理接入上游 watermarks-remover 服务

- **客户端适配上游 API 契约**：上游 [guillaumemeyer/watermarks-remover](https://github.com/guillaumemeyer/watermarks-remover) 的 `/inspect`、`/clean` 接收 base64 文件负载（`{"file": ..., "name": "input.txt"}`），文本清理结果以 base64 `file` 字段回传；旧版内置 sidecar 的 `{"kind":"text","text":...}` 与 `{"text":...}` 契约继续兼容，两种响应形状均支持。
- **一键部署接入官方镜像**：`install.sh` 与参考 compose 的 `watermarks-remover` 服务改用上游官方镜像 `ghcr.io/guillaumemeyer/watermarks-remover`（内置 exiftool/qpdf/Ghostscript/ffmpeg 文件元数据清理），凭 `WATERMARKS_SERVICE_TOKEN` 启用 Bearer 认证，与 Runtime 水印插件共用同一 Token。文本 Layer B 智能改写为可选能力，需另行配置 `WATERMARKS_REWRITE_*` LLM 后端。

## [0.6.2] - 2026-09-09

### 客户端启动直落登录页与发布流程修正

- **APK 启动不再跳过登录**：客户端构建的根路径此前固定重定向到 `/dashboard`，而静态导出的认证门禁无法在预渲染阶段运行，未登录用户直接进入只有壳子的首页（数据请求全部失败）。客户端构建根路径现在重定向到 `/auth/runtime-login`，服务端构建行为不变。
- **发布流程说明**：`create_doc_release.py` 要求 tag 版本必须存在对应 CHANGELOG 段落，重构建标签也需同步补段；自本版本起恢复正常语义化发版。

## 重构建标签 v0.6.1-rebuild2 - 2026-09-10

> CI 重构建标签：内容与 [0.6.1] 完全一致，用于在配置固定签名密钥后重新产出可安装的 APK。该版本 APK 已验证为 v2 固定签名，可正常安装；其发布流程中发行注记步骤失败仅影响 Release 描述文本，不影响 APK 附件。

## [0.6.1] - 2026-09-09

### 手机客户端连接修复与 APK 固定签名

- **修复手机客户端"测试并保存节点"报 Failed to fetch**：打包客户端此前以 `https://localhost` 作为 WebView 源，而自建节点均为 `http://` 局域网地址，属混合内容（Mixed Content）被 WebView 静默拦截。客户端源改为 `http` scheme，节点探测与账密绑定 Token 请求恢复正常（服务端 CORS 已验证对 http 源完全放行）。
- **修复手机端无法删除节点**：节点切换器与登录页节点列表的删除按钮此前仅在鼠标悬停时显示（触屏设备没有悬停，按钮永远不可见），且当前选中节点只显示对勾不显示删除。现删除按钮常显、选中节点亦可删除，并加大触控目标。
- **APK 固定签名密钥**：CI 此前使用 runner 临时生成的 debug keystore，每次构建签名都不同，覆盖安装必然提示"签名不同"。构建流程改为 `assembleRelease` 并通过 GitHub Secrets 注入固定签名密钥（未配置时自动回退调试签名并给出警告）。**注意：旧版本 APK 首次升级到固定签名版本仍需卸载重装一次，此后即可正常覆盖升级。**

## [0.6.0] - 2026-09-09

### 手机客户端可用性修复与部署链路闭环

- **修复手机端插件页"此页面无法加载"**：APK 采用 Next.js 静态导出，动态插件页路由只能预渲染构建期已知的固定组合，外部 ZIP 插件与 `community`、`pay` 等页面的侧栏链接在客户端内全部 404。新增 `/dashboard/runtime/plugin-view` 查询参数路由：打包客户端（`NEXT_PUBLIC_CLIENT_BUILD=true`）将插件页链接解析到该单一预渲染路由、运行时按查询参数渲染任意插件页；服务端部署保持原有语义化路径完全不变。
- **手机客户端登录页节点选择器与 Bearer Token 鉴权**：打包客户端运行于 `capacitor://` 源，`SameSite=lax` 会话 Cookie 无法跨源携带，此前 APK 既不能切换服务器节点也无法完成登录。登录页新增节点选择器（仅客户端构建渲染），登录改走 `POST /api/auth/token` 换取长期 Bearer Token（设计上豁免 CSRF）并持久化到节点条目，后续请求统一以 `Authorization: Bearer` 鉴权。
- **消除健康检查高延迟**：`/api/health` 每次轮询都对整个 SQLite 数据库执行 `PRAGMA quick_check` 全量完整性扫描（92MB 库约 300ms），导致节点延迟探测恒为扫描耗时而非真实网络延迟。改为 5 分钟 TTL 缓存 + 过期后台无阻塞刷新；管理台运行概览仍强制真实扫描，保留权威完整性结论；`close()` 取消后台刷新任务，避免对已关闭连接执行语句。
- **表情包模板"自动预览"开关（默认关闭）**：模板列表页原本对全部数百个模板批量加载预览图，网络压力过大。新增 `auto_preview` 配置项：默认关闭时目录接口不下发 `preview_url`、列表以"预览"文字按钮呈现，点击弹窗仍可按需加载单张预览（服务端磁盘持久缓存）；开启后恢复缩略图批量加载。
- **一键部署脚本接入新版控制台**：`install.sh` 此前生成的 `compose.yaml` 缺少 `dashboard` 服务，`RUNTIME_DASHBOARD_URL` 未设置导致全新安装静默回退旧版 legacy 控制台。现自动推导与所选镜像源配套的 dashboard 镜像、生成 `dashboard` 服务（同源代理 + `RUNTIME_API_ORIGIN` 回连）；在 dashboard 镜像尚未发布的过渡期自动降级 legacy 控制台并提示后续一键切换命令，不中断安装。
- **CI 发布 dashboard 镜像**：发布流程新增 `ghcr.io/chinachani/qq-runtime-dashboard` 的构建（Node 22 + standalone 产物）与版本/`latest` 双标签推送，补齐"打 tag → 双镜像发布 → 一键安装拉取"完整链路。
- **修复绘图插件积分结算残留死代码**：清理 `drawimg` 配置导出与积分结算路径中的无用 `users` 字典与未定义 `cs` 引用（后者在 `except` 块内触发 `NameError`，会把已吞掉的业务异常升级为 500）。

## [0.4.0] - 2026-09-06

### 新增功能与架构演进

- **Linux 服务器 Docker 智能一键部署脚本 (`install.sh`)**：支持无代码环境下一键智能部署，具备操作系统架构自检、Docker 与 Docker Compose 守护进程运行检测与自启/安装引导；内置 4 大国内高速加速源（`ghcr.1ms.run`、`ghcr.nju.edu.cn`、`ghcr.milu.moe`、`docker.m.daocloud.io`）及官方源交互切换；自动生成强随机主密钥、高强度安全初始管理员凭证、生产级 `compose.yaml` 与 `.env`；集成容器健康检查轮询与精美控制台高亮卡片。
- **全端原生接入底座与 API Token 鉴权体系**：
  - 新增 `api_tokens` 数据表与持久化方法，支持长期与限期 API Token 的安全哈希存储（SHA-256）；
  - `current_user` 认证机制升级：支持 `Authorization: Bearer <TOKEN>` 头部鉴权，无缝适配 Tauri 桌面端与 Capacitor 移动端原生直连；
  - Bearer Token 认证自动豁免 CSRF 检查，确保客户端跨源调用的合规与安全；
  - 提供 `POST /api/auth/token` 账密换取 Token 开放接口，以及管理台 Token 签发、查看与撤销路由（`/api/system/tokens`）；
  - 引入 `CORSMiddleware` 跨源访问支持，打通桌面端（`tauri://*`）与移动端（`capacitor://*`）跨域调用。

## [0.3.4] - 2026-09-06

### 修复与控制台优化

- **一键更新伴生服务连接与重启容错优化**：重构伴生服务下发逻辑，区分网络连接与拉取等待阶段；将伴生服务在执行拉取或重启时引起的读取超时（`ReadTimeout`）和容器销毁连接关闭正确识别为“已成功受理并正在重启”，杜绝误报“无法连接更新伴生服务”；新增 45 秒防抖和近期重启状态感知。
- **更新日志弹窗宽度与高度滚动条优化**：弹窗宽度加宽至 `max-w-3xl`，日志内容区明确限定高度（`max-h-[55vh]`）并提供纵向平滑滚动条，长日志排版更舒展。
- **系统与管理卡片加宽布局**：调整桌面端布局为 12 列网格，将【版本与更新】卡片宽度增加至 5 栏（加宽约 25%），镜像加速源选择、拉取命令复制与日志预览更加宽敞。

## [0.3.3] - 2026-09-06

### 插件系统与服务优化

- **绘图服务风格预设表格化呈现**：将 `/dg style` 预设列表重构为工整紧凑的 Markdown 表格展示（包含预设名称、提示词预览、选中状态三列）；彻底过滤换行符与表格分割符，提示词预览截取前 40 字符并加省略号，消除数百字提示词刷屏困扰。
- **预设详情独立查看指令**：新增 `/dg style view <预设名>` 指令，支持随时以富文本卡片查看长文本预设的完整提示词内容。
- **插件数据目录严格隔离**：彻底修复绘图插件持久化路径，严禁向插件代码安装根目录（`./data/plugins/`）写入运行期文件，统一重定向至 `data/plugin-data`，启动时自动清理历史遗留数据，彻底解决由此引起的包指纹 SHA256 校验失败问题。
- **插件指纹校验白名单增强**：`_directory_digest` 在校验包完整性时，自动跳过运行期生成的 `data/`、`config/`、`logs/`、`cache/` 目录以及临时文件（`*.log`、`*.tmp`、`*.sqlite*` 等），杜绝动态数据破坏代码包校验。
- **启动恢复容错与重试保障**：在开机启动恢复（`restore_enabled`）阶段遭遇瞬态超时或异常时，仅在内存中标记错误，**绝不在 SQLite 数据库中覆盖为 `load_failed`**，保留用户的 `enabled` 启动意图；并增加 1 秒延迟自动重试机制。
- **沙箱 Worker 启动握手超时放宽**：将 Worker 进程初始化握手超时上限从 10 秒放宽至 30 秒（`startup_timeout_seconds`），杜绝低功耗小主机重启并发拉起多个插件时因 CPU 瞬时高负载导致启动超时误报。

## [0.3.2] - 2026-09-06

### 修复与运维

- 修复 Watchtower 一键更新中断 Bug：修复伴生更新服务默认扫描全部容器、在拉取私有本地构建的 Dashboard 容器时遭遇权限拒绝导致更新流程中断的问题。
- 引入容器精准更新隔离标签：在 Compose 配置中为 Runtime 容器添加专属更新标签（`com.centurylinklabs.watchtower.enable=true`），并在 Watchtower 启用 `WATCHTOWER_LABEL_ENABLE: "true"`，杜绝误更新与权限拒绝异常。
- 确保自动更新平滑闭环：容器镜像目标规范绑定为 `latest` 标签，实现管理台点击一键更新后自动拉取新版本并重启服务。

## [0.3.1] - 2026-09-06

### 管理台与系统

- 版本发布日志体验优化：当系统处于最新版本时，自动展示本次版本的发布与更新日志，不再展示“暂无待更新日志”。
- 本地日志回退保障：增加内置 `CHANGELOG.md` 提取机制，在 GitHub API 响应超时或受限时自动回退读取本地日志，确保离线与弱网环境下的日志完整可见。
- GHCR 国内镜像源加速：新增 4 大常用国内镜像加速源（`ghcr.1ms.run`、`ghcr.nju.edu.cn`、`ghcr.milu.moe`、`docker.m.daocloud.io`），在管理台版本卡片与更新弹窗中支持一键切换与拉取命令联动复制。
- 镜像打包与环境变量优化：Docker 镜像打包内置 `CHANGELOG.md`，Compose 配置支持灵活切换镜像加速 Registry。

## [0.3.0] - 2026-09-06

### 管理台与系统

- 版本与更新模块重构：移除 SQ 授权中心干扰，统一采用公开仓库 Runtime-doc 的 GitHub Releases 作为权威版本源。
- 版本日志 Markdown 结构化美化：引入富文本渲染，卡片内设限制高度（`max-h-24`）与底部渐隐遮罩。
- 完整更新日志 Dialog 弹窗：支持弹窗浏览完整日志记录、镜像拉取命令一键复制以及 GitHub Releases 快速入口。
- 一键自动更新机制（Watchtower 伴生服务）：支持通过前端管理台直接下发更新指令，平滑拉取新镜像并重启服务，配合前端自动轮询健康状态与重载页面。

### 插件与服务

- word2ppt：计费前置校验修复，积分不足时提前拦截任务创建，避免产生无效执行记录。
- word2ppt：优化反代大模型接口异常处理与状态反馈。

## [0.2.1] - 2026-09-05

### 安全与发行

- 发行镜像执法链 Cython 编译（二进制分发）+ 完整性自检；厂商公钥与网关公钥构建期烧录。
- 授权响应网关 Ed25519 签名：编译客户端强制验签（默认开启，环境变量仅可轮换）。
- 商业插件厂商签名强制：`[license]` 插件必须厂商签名方可激活，开发者模式与环境变量均不可绕过。
- 版本检测与更新日志迁移至公开仓库 Runtime-doc；GitHub Actions 构建发行镜像并推送 GHCR。

### 插件

- word2ppt：管理面板增强、Python 依赖管理与模型列表接口。
- 官方插件前端 SDK（RuntimePluginBridge）。
- 沙箱与插件生命周期行为调整。

## [0.2.0] - 2026-09-06

### 框架

- 商业授权体系：机器人绑定授权、功能位（权益包）、3 天离线宽限、心跳续期。
- 未授权实例受限模式：仅允许身份与授权诊断指令。
- 管理台 v2：极简主题、插件/媒体分页筛选、会话备注与预览、审计与版本更新检查。
- 插件系统：ZIP 包安装/升级/卸载、进程级沙箱（bubblewrap/seccomp）、
  权限与依赖白名单、网页面板桥接。
- 积分与支付桥（内置 runtime.pay）：订单创建/查询/退款/兑换。
- 内置插件：社区管理、权限管理、MD 卡片、绘图、表情包、水印、NA 等。

### 安全与发行

- 授权响应 Ed25519 签名（网关加签，客户端烧录公钥验签）。
- 商业插件厂商签名强制：`[license]` 插件必须厂商签名方可激活，
  开发者模式与环境变量均不可绕过。
- 发行镜像执法链 Cython 编译（二进制分发）+ 完整性自检。

### 已知限制

- word2ppt 的单页重做与 AI 配图为降级实现，待后续版本补全。